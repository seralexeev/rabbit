# 🔗 Тестирование подключения робота

## 🎯 Что будет происходить при переподключениях

### Когда браузер перезагружается (refresh):
1. **🔴 Браузер отключается** - робот обнаружит потерю соединения через watchdog
2. **🧹 Автоматическая очистка** - робот закроет старое WebRTC соединение
3. **🔄 Переподключение** - браузер запросит новое соединение через `request_offer`
4. **🟢 Новое соединение** - робот создаст новое WebRTC соединение

### Когда робот перезапускается:
1. **📤 Новые offers** - робот отправляет новые WebRTC предложения каждые 10 секунд
2. **🔄 Автоматический сброс** - браузер автоматически сбросит старое соединение при получении нового offer
3. **🟢 Восстановление** - соединение будет восстановлено без участия пользователя

## 🚀 Инструкции по запуску и тестированию

### 1. Запустить сервер сигнализации:
```bash
yarn workspace @rabbit/server dev
```

### 2. Запустить веб-интерфейс:
```bash
yarn workspace @rabbit/web dev
```

### 3. Запустить робота:
```bash
cd workspaces/rabbit
uv run src/client.py
```

## 🧪 Сценарии тестирования

### ✅ Тест 1: Обычное подключение
1. Запустите все компоненты
2. Проверьте логи - должны увидеть:
   - `🟢 WebSocket connected`
   - `🟢 WebRTC connection established`
   - `🟢 Data channel opened`

### ✅ Тест 2: Перезагрузка браузера
1. Перезагрузите страницу браузера (F5/Cmd+R)
2. Проверьте логи робота:
   - `🟡 WebRTC connection lost, cleaning up...`
   - `🔄 Browser requested new offer - resetting connection`
   - `🟢 WebRTC connection established`

### ✅ Тест 3: Перезапуск робота
1. Остановите робота (Ctrl+C)
2. Запустите робота снова
3. Соединение должно восстановиться автоматически через несколько секунд

### ✅ Тест 4: Отправка сообщений
1. Нажмите кнопки на геймпаде
2. Проверьте логи робота - должны увидеть:
   - `🎮 CONTROLLER: Buttons: A, B`
   - `🎮 CONTROLLER: Sticks: L(0.45, -0.78)`

## 🔧 Улучшения в коде

### Робот (`client.py`):
- ✅ **Connection Watchdog** - мониторинг состояния соединения каждые 5 секунд
- ✅ **Автоматический сброс** - при потере соединения автоматически создается новое
- ✅ **Корректная очистка** - все ресурсы освобождаются при отключении
- ✅ **Обработка переподключений** - корректная реакция на запросы нового соединения

### Браузер (`useWebRTC.tsx`):
- ✅ **Автоматический сброс** - при получении нового offer всегда создается новое соединение
- ✅ **Обработка ошибок** - корректная обработка ошибок соединения
- ✅ **Переподключение** - автоматический запрос нового соединения при подключении WebSocket

## 📊 Логи для мониторинга

### Ожидаемые логи при нормальной работе:
```
🟢 Connected to WebSocket server
🟢 WebRTC connection established  
🟢 Data channel opened and ready to receive data
🎮 CONTROLLER: Buttons: A | Sticks: L(0.25, -0.50)
```

### Логи при переподключении браузера:
```
🟡 WebRTC connection lost, cleaning up...
🔄 Browser requested new offer - resetting connection
🟢 WebRTC connection established
🟢 Data channel opened and ready to receive data
```

### Логи при перезапуске робота:
```
🤖 Starting robot client...
🔌 Connecting to ws://localhost:3000?role=robot
🟢 Connected to WebSocket server
📤 WebRTC offer created and sent
```
