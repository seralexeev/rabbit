# Робот клиент WebRTC

Этот файл реализует WebRTC клиент для робота, который:

1. Подключается к WebSocket серверу
2. Устанавливает WebRTC соединение с браузером
3. Получает команды контроллера через data channel
4. Логирует все команды от контроллера

## Установка зависимостей

```bash
cd workspaces/rabbit
uv sync
```

## Запуск

1. Сначала запустите WebSocket сервер:
```bash
cd workspaces/server
npm install
npm start
```

2. Затем запустите веб-интерфейс:
```bash
cd workspaces/web
npm install
npm run dev
```

3. Наконец, запустите робот клиент:
```bash
cd workspaces/rabbit
uv run src/client.py
```

## Как это работает

1. Робот подключается к WebSocket серверу как "robot"
2. Браузер подключается как "browser" 
3. **Робот создает WebRTC offer и отправляет его браузеру**
4. **Браузер получает offer и отвечает answer**
5. После установки соединения, команды геймпада передаются через data channel
6. Робот логирует все полученные команды

Эта архитектура позволяет роботу быть сервером WebRTC соединения, что более логично для управления роботом.

## Примеры логов

```
🎮 КОНТРОЛЛЕР: Кнопки: cross, l1 | Стики: L(0.45, -0.32), R(0.12, 0.89)
🎮 КОНТРОЛЛЕР: Стики: L(-0.78, 0.23)
🎮 КОНТРОЛЛЕР: Кнопки: triangle
```

## Интеграция с роботом

В функции `process_robot_commands()` можно добавить логику управления роботом:
- Движение на основе левого стика
- Поворот камеры на основе правого стика
- Специальные действия по кнопкам
